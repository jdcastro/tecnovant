{% extends "base.j2" %}
{% macro info_card(title, value, description) %}
    <div class="bg-gray-50 p-4 rounded-lg shadow text-center">
        <p class="text-lg font-semibold">{{ title }}</p>
        <p class="text-2xl font-bold">{{ value }}</p>
        <p class="text-gray-500">{{ description }}</p>
    </div>
{% endmacro %}

{% block content %}
        <div class="flex justify-between items-center mb-4">
            <h1 class="text-2xl font-bold">Informes de Análisis</h1>
            <div>
                <span class="bg-gray-200 px-4 py-2 rounded mr-2">Informes generados {{ total_informes }}</span>
                <button class="bg-blue-600 text-white px-4 py-2 rounded">+ Crear Nuevo Informe</button>
            </div>
        </div>

{# <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
    {{ info_card("Total de Informes", "5", "4 completados, 1 borrador") }}
    {{ info_card("Análisis Foliares", "4", "80% del total de informes") }}
    {{ info_card("Análisis de Suelo", "4", "80% del total de informes") }}
</div> #}

        <div class="bg-gray-50 p-4 rounded-lg shadow">
            <h2 class="text-lg font-semibold mb-2">Listado de Informes</h2>
            <table class="w-full border rounded">
                <thead>
                    <tr class="bg-gray-200">
                        <th class="p-2">Título</th>
                        <th class="p-2">Finca / Lote</th>
                        <th class="p-2">Cultivo</th>
                        <th class="p-2">Fecha</th>
                        <th class="p-2">Tipo</th>
                        <th class="p-2">Autor</th>
                        <th class="p-2">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="border">
                        <td class="p-2">Informe de Análisis - El nuevo rocío (Lote 1)</td>
                        <td class="p-2">El nuevo rocío / Lote 1</td>
                        <td class="p-2">Maíz</td>
                        <td class="p-2">17/03/2025</td>
                        <td class="p-2">🌱</td>
                        <td class="p-2">Juan Pérez</td>
                        <td class="p-2 text-blue-500 cursor-pointer">👁️ Ver</td>
                    </tr>
                </tbody>
            </table>
        </div>
{% endblock %}